// Base URLs
export const BASE_API_URL = "https://dummyjson.com"

// API Paths
export const API_PATHS = {
  PRODUCTS: "/products",
  CARTS: "/carts",
  USERS: "/users",
  AUTH: "/auth",
  LOGIN: "/login",
}

// Tokens
export enum TOKENS {
  ACCESS_TOKEN = "authToken",
}